/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number}
 */
var search = function(nums, target) {
    if (nums[left] < nums[right]){
      while(middle !== target && left <= right){
  
      }
  
    }else if (nums[left] < nums[right]){
  
  
    }  
  };
  /*
  timeComplexity (logn)
  logn = x
  base is 2
  2^x = */