/**
 * @param {number[]} numbers
 * @param {number} target
 * @return {number[]}
 */
var twoSum = function(numbers, target) {
    // 2 ptr at extreme end
    //biggest no, smallest no
    add no. to get current sum
    Do addition inn while (low != high){//add}
    if sum == result else higher than target decrease high
    lower than the target increase low
};

/**
 * Optimisation 
 compare lists
 convert it two string 
 compare string
 return the string
 */