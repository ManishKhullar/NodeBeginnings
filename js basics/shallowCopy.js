const ingredientsList = ["noodles", {list:["eggs", "flour", "water"]}];
const ingredientsListCopy = ingredientsList;


// ????? DIfference between copying directly vs Copying using Array.from()
console.log(ingredientsListCopy);
console.log(ingredientsListCopy);